[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "router_rs"
version = "0.1.0"
description = "Arke deterministic router — Rust/PyO3 extension"
requires-python = ">=3.11"
