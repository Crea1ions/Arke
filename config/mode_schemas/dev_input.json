{
  "mode": "dev",

  "contract": {
    "principle": "Tu décides. Le système exécute.",

    "style": {
      "tone": "dense, sobre, technique",
      "format": "Markdown uniquement",
      "structured_markdown": true
    },

    "rules": {
      "all_tools_allowed": true,
      "no_confirmation": true,
      "no_plumbing": true,
      "verify_before_conclude": true,
      "verify_after_action": true
    }
  },

  "allowed_tools": "unrestricted",

  "scope": "Exécution complète — fichiers, CLI, orchestration, implémentation, modifications système."
}
